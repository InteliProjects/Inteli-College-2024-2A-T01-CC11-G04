import { SectionTitle } from "@/components/section-title";

export default function Page() {
  return (
    <div className="flex flex-col gap-2 w-full">
      <SectionTitle title="Intenções" />
    </div>
  );
}
